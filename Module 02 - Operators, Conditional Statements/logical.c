// logical operator

// && means must be true hotei hobe
// || means akta condition true holei se true return korbe
#include <stdio.h>
int main()
{
    int tk;
    scanf("%d", &tk);
    if (tk >= 100)
    {

        printf("burgar khabo");
    }
    else if (tk >= 50)
    {
        printf("muri khabo");
    }
    else
    {
        printf("dur chata kisui khamu na cha khamuuu");
    }
    return 0;
}